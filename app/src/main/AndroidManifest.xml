<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="dream.quqiongyou">
<uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.widget.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".main.widget.MainActivity"
            android:launchMode="singleInstance"/>
        <activity android:name=".register.view.RegisterActivity"/>
        <activity android:name=".origination.view.OriginationActivity"/>
        <activity android:name=".fuckticket.view.FuckticketActivity"/>
        <activity android:name=".postdetail.view.PostDetailActivity"/>
        <activity android:name=".post.view.PostActivity"/>
    </application>

</manifest>